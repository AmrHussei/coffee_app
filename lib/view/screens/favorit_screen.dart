import 'package:flutter/material.dart';

class FavorivesScreen extends StatelessWidget {
  const FavorivesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('fafoooooo'),);
  }
}
